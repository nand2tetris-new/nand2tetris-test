package Misc;

import java.util.Scanner;

public class Check {
    Scanner in = new Scanner(System.in);
    String c = in.nextLine();
    
    // Should write a function that checks input
    // and runs a method based on it
}
